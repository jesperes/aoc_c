#ifndef COMPARE_H
#define COMPARE_H

int int_compare_desc(const void *p1, const void *p2);
int int_compare_asc(const void *p1, const void *p2);
int char_compare_asc(const void *p1, const void *p2);

#endif
